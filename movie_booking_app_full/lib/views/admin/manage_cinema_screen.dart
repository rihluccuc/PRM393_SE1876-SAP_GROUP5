import 'package:flutter/material.dart';

/// ManageCinemaScreen - Màn hình quản lý rạp
class ManageCinemaScreen extends StatelessWidget {
  const ManageCinemaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Quản lý rạp'),
      ),
      body: const Center(
        child: Text('Manage Cinema Screen - Coming Soon'),
      ),
    );
  }
}
