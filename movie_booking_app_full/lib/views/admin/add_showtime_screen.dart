import 'package:flutter/material.dart';

/// AddShowtimeScreen - Màn hình thêm suất chiếu
class AddShowtimeScreen extends StatelessWidget {
  const AddShowtimeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Thêm suất chiếu'),
      ),
      body: const Center(
        child: Text('Add Showtime Screen - Coming Soon'),
      ),
    );
  }
}
